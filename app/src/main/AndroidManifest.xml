<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.tailor">
    <!-- Permissions should be declared directly under the manifest tag -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="Tailors"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Tailor"
        tools:targetApi="31">
        <activity
            android:name=".ViewAll"
            android:exported="false" />
        <activity
            android:name=".CustomAdapter1"
            android:exported="false" />
        <activity
            android:name=".list_item_layout"
            android:exported="false" />
        <activity
            android:name=".selectDateActivity"
            android:exported="false" />
        <activity
            android:name=".activity_select_date"
            android:exported="false" />
        <activity
            android:name=".activity_selected_date"
            android:exported="false" />
        <activity
            android:name=".selectDate"
            android:exported="false" />
        <activity
            android:name=".AddBottonActivity"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="New Regular Tailor">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>